<!DOCTYPE html>
<html lang="es">

  <%- include("../partials/head", {title: "Perfil", uniqueStyle: "style-perfil.css"}) %>

  <body>
    <%- include("../partials/header") %>
    <%- include("../partials/whatsapp") %>
    <main>
      <div class="wrapper">
          <div class="left">
              <img src=<%= "/img/users/" + locals.session.user.img_path %> 
              alt="user" width="100">
              <h2><%= locals.session.user.first_name %> <%= locals.session.user.last_name %></h2>
          </div>
          <div class="right">
              <div class="info">
                  <h2>Información</h2>
                  <div class="info_data">
                       <div class="data">
                          <h4>Email</h4>
                          <p><%= locals.session.user.email %></p>
                       </div>
                       <div class="data">
                         <h4>
                           <% if (locals.session.user.category == "vendor") { %>
                          Vendedor
                        <% } %>
                        <% if (locals.session.user.category == "buyer") { %>
                          Comprador
                        <% } %>
                         </h4>
                       </div>
                  </div>
              </div>
            
            
              <div class="edit">
                <a href="/users/profile-edit/" class="btn-1">Editar Perfil</a>
              </div>
          </div>
      </div>
    </main>
  </body>
    <%- include("../partials/footer") %>
  </body>

</html>